.TH simple_shell 1 "Jan 2026" "v1.0" "User Commands"
.SH NAME
simple_shell \- simple UNIX command interpreter
.SH SYNOPSIS
.B simple_shell
[\fIcommand\fR]
.SH DESCRIPTION
The
.B simple_shell
program is a simplified UNIX command interpreter written in C. It reads
lines from standard input (interactive mode) or from a pipe/file (non-interactive
mode), parses them, and executes the corresponding commands.

It supports basic command execution, environment handling, and built-in
commands. This implementation was created as part of the Holberton School
curriculum to better understand internal shell behavior, process creation,
and environment management.

.TP
.B Interactive mode
Displays a prompt "$ " and waits for the user to enter commands.
.TP
.B Non-interactive mode
Reads commands from standard input (for example, via echo or file redirection)
and executes them sequentially.

.SH BUILT-IN COMMANDS
The following built-in commands are supported:

.TP
.B exit
Exits the shell. If an argument is provided (for example, "exit 3"), the shell
returns that value as the exit status.

.TP
.B env
Prints the current environment variables.

.SH USAGE
In interactive mode:

.nf
$ ./hsh
$ ls -l
$ env
$ exit
.fi

In non-interactive mode:

.nf
$ echo "ls -la" | ./hsh
.fi

.SH EXIT STATUS
On successful execution, the shell returns the exit status of the last executed
command.

If a command is not found, the shell prints an error message similar to:

.PP
./hsh: 1: command: not found

.SH ERRORS
The behavior is undefined if:

.IP \[bu]
Memory allocation fails.
.IP \[bu]
Input exceeds the maximum supported length.

.SH SEE ALSO
sh(1), bash(1)

.SH AUTHORS
Lorenzo Anselme and Ethane Zimmermann
